# Public (sent to the browser; only NEXT_PUBLIC_* are safe)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_URL=

# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Anthropic / Gateway. Prefer AI Gateway (Vercel OIDC inside a deploy, or
# AI_GATEWAY_API_KEY locally); ANTHROPIC_API_KEY is the fallback when neither
# is present (e.g. `npm run dev` against a personal key).
ANTHROPIC_API_KEY=
AI_GATEWAY_API_KEY=

# Model slugs (override per role). Defaults are now Mistral Small.
MODEL_ANALYSIS=mistral-small-latest
MODEL_FAST=mistral-small-latest
MODEL_VISION=mistral-large-latest
OPENAI_API_KEY=
MISTRAL_API_KEY=
# Anthropic still supported — leave key blank if you only use OpenAI/Mistral.
ANTHROPIC_API_KEY=

# Supabase (server-only)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Internal Python backend (server-to-server bearer; same value as
# KNOW_INTERNAL_BACKEND_TOKEN on the backend)
INTERNAL_BACKEND_URL=https://your-backend.example.com
INTERNAL_BACKEND_TOKEN=

# Vercel KV (auto-injected on Vercel deploys with KV linked)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Vercel Cron secret (for /api/cron/* auth)
CRON_SECRET=

# Google Drive integration (optional — when set, an "Open from Google
# Drive" button appears on the dashboard upload landing and in the
# in-paper "Add Paper" popover). Browser-side only: client ID + API key
# + app ID are restricted to the deployment origin via Google Cloud
# console. Uses the `drive.file` scope so users grant access to one
# file at a time. Leave blank to hide the button.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
NEXT_PUBLIC_GOOGLE_API_KEY=
NEXT_PUBLIC_GOOGLE_APP_ID=
